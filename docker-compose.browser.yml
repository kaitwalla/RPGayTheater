services:
  app:
    ports: !reset []
    environment:
      APP_URL: http://app:8000
    depends_on:
      minio:
        condition: service_started

  postgres:
    ports: !reset []

  redis:
    ports: !reset []

  minio:
    ports: !reset []
